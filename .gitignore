# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

.env

# Prisma / local DB
/prisma/dev.db
/prisma/dev.db-journal
/prisma/backups/
/src/generated/

# Raw third-party dumps (not committed)
/data/_raw/

# Lexique minimum B1 : PDF sous droit d'auteur (jamais committé) + l'extraction qui en dérive
# (mots + traductions, déjà appliquée aux deux DB via scripts/import-b1-lexicon.ts — pas besoin
# du fichier en historique git pour que l'app fonctionne). Voir docs/adr/0003.
/data/B1/

# Legacy JSON state (migré en DB)
/prisma/_legacy-json/

# but keep the example
!.env.example
